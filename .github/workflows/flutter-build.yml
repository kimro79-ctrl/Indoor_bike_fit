name: BikeFit Final Ultra Build - GOOGLE CONSOLE PASS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Setup Java & Flutter (3.27.1)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: 🐦 Flutter 설정
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🏗️ 환경 재건 및 V2 강제 설정
        run: |
          # 1. 안드로이드 폴더 삭제 후 재생성
          rm -rf android
          flutter create . --platforms android --org com.overthebike --project-name over_the_bike_fit
          flutter pub get
          
          # 2. V2 선언이 포함된 Manifest 직접 생성
          cat <<EOF > android/app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.overthebike.over_the_bike_fit">
              <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
              <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
              <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
              <application android:label="Indoor_fit_app" android:icon="@mipmap/ic_launcher">
                  <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop" android:theme="@style/LaunchTheme">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <meta-data android:name="flutterEmbedding" android:value="2" />
              </application>
          </manifest>
          EOF

      - name: 🛠️ Gradle 서명 강제 제거 (사용자 제안 로직 적용)
        run: |
          # 💡 [핵심] sed 대신 build.gradle 파일의 서명 로직을 null로 직접 수정합니다.
          # 이 작업으로 인해 플러터 빌드 중 발생하는 자동 서명이 물리적으로 불가능해집니다.
          FILE="android/app/build.gradle"
          sed -i 's/signingConfig signingConfigs.debug/signingConfig null/g' $FILE
          # release 설정이 있을 경우를 대비해 모두 null 처리
          sed -i 's/signingConfig signingConfigs.release/signingConfig null/g' $FILE
          
          # MultiDex 추가
          sed -i '/defaultConfig {/a \        multiDexEnabled true' $FILE

      - name: 🚀 Build Pure Release AAB
        # 서명 설정이 null이므로 '서명되지 않은' 정식 AAB가 생성됩니다.
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: 🖋️ Sign AAB (새 업로드 키로 단일 서명 수행)
        run: |
          # 💡 도장이 없는 깨끗한 release 파일에 사용자님의 새 키로 도장을 찍습니다.
          # 비밀번호: Sodomaki80 / 별칭: fresh-upload-key
          jarsigner -keystore release.jks \
          -storepass Sodomaki80 -keypass Sodomaki80 \
          build/app/outputs/bundle/release/app-release.aab fresh-upload-key

      - name: 📦 최종 AAB 추출
        uses: actions
