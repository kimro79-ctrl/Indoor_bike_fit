name: BikeFit - GET MY AAB REAL FINAL

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 17 및 Flutter 설정
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Flutter 설정
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🧹 V1 잔재 소탕 및 MainActivity 고정
        run: |
          flutter clean
          # V1 에러를 유발하는 폴더를 아예 날려버립니다.
          rm -rf android/app/src/main/java
          
          # MainActivity.kt 위치를 최신 규격으로 생성
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

      - name: 🔑 키스토어 및 서명 설정
        run: |
          # JKS 파일 위치 보장
          cp release.jks android/app/release.jks || echo "JKS already in android/app"
          
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          
          flutter pub get

      - name: 🚀 AAB 빌드 실행 (방어 모드)
        run: |
          # 빌드 직전, 다시 생겨났을지 모를 V1 파일을 마지막으로 제거
          rm -rf android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java || true
          flutter build appbundle --release --no-tree-shake-icons

      - name: 📦 [핵심] 모든 경로에서 AAB 낚아채기
        uses: actions/upload-artifact@v4
        with:
          name: OverTheBike_AAB_SUCCESS
          # 경로를 더 넓게 잡아 어디에 생성되든 다 가져옵니다.
          path: |
            build/app/outputs/bundle/release/*.aab
            build/**/*.aab
          if-no-files-found: error
