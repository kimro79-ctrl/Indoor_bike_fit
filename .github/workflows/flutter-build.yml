name: BikeFit Final Success
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 메인 루트의 JKS 강제 주입 및 설정 복구
        run: |
          # 1. 안드로이드 설정 강제 초기화 (apilevel 등 꼬인 설정 해결)
          flutter create . --platforms android --overwrite

          # 2. [핵심] 메인 루트에 있는 진짜 release.jks를 앱 폴더로 강제 복사
          # android/app에 있던 기존 찌꺼기는 여기서 완전히 덮어씌워집니다.
          mkdir -p android/app
          cp release.jks android/app/release.jks

          # 3. key.properties 생성 (fresh-upload-key 별칭 고정)
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 4. 빌드 서버가 임시 키를 쓰지 못하도록 release 서명 강제 고정
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 결과물 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_FINAL_AAB
          path: build/app/outputs/bundle/release/app-release.aab
