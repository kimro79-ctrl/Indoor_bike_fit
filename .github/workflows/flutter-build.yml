name: BikeFit Google Console Final
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 별칭 강제 매칭 및 서명 고정
        run: |
          # 1. 안드로이드 설정 초기화 (V1 에러 방지)
          flutter create . --platforms android --overwrite

          # 2. JKS 복사
          mkdir -p android/app
          cp release.jks android/app/release.jks

          # 3. [핵심] 파일 분석 결과를 바탕으로 '진짜 이름' 강제 주입
          # 만약 fresh-upload-key가 안되면 파일 내부에 숨겨진 이름을 시스템이 찾도록 처리
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 4. 빌드 서버가 임시 키를 생성하지 못하도록 강제 설정
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★이 파일을 다운로드하여 구글에 올리세요★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_SUCCESS_AAB
          path: build/app/outputs/bundle/release/app-release.aab
