name: BikeFit FULL Android V2 Rebuild Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Setup Flutter (V2 안정판)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'

      - name: 🧹 Full Android Rebuild (V2 Embedding)
        run: |
          echo "🧼 Cleaning old Android structure..."
          rm -rf android
          flutter clean
          rm -rf ~/.pub-cache
          flutter pub cache repair || true

          echo "🧩 Recreating Android folder from V2 template..."
          flutter create .
          echo "✅ Android folder recreated successfully."

          echo "🧩 Reconfiguring package name..."
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity() {}
          EOF

          echo "🧩 Patching AndroidManifest.xml..."
          cat <<EOF > android/app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android"
              package="com.overthebike.over_the_bike_fit">
              <application
                  android:label="over_the_bike_fit"
                  android:name="\${applicationName}"
                  android:icon="@mipmap/ic_launcher">
                  <activity
                      android:name=".MainActivity"
                      android:exported="true"
                      android:launchMode="singleTop"
                      android:theme="@style/LaunchTheme"
                      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|screenLayout|density|fontScale"
                      android:hardwareAccelerated="true"
                      android:windowSoftInputMode="adjustResize">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <meta-data
                      android:name="flutterEmbedding"
                      android:value="2" />
              </application>
          </manifest>
          EOF

          echo "✅ AndroidManifest.xml and MainActivity re-created (V2)."

      - name: 🔐 Setup Keystore
        run: |
          echo "🔑 Creating key.properties..."
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          if [ ! -f "android/app/release.jks" ]; then
            cp $GITHUB_WORKSPACE/release.jks android/app/release.jks
          fi

      - name: 📦 Install dependencies
        run: |
          flutter pub get

      - name: 🚀 Build AAB (V2)
        run: |
          echo "🚀 Building final AAB in clean V2 environment..."
          flutter build appbundle --release --no-tree-shake-icons

      - name: 📦 Upload AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_V2_Rebuild_AAB
          path: build/app/outputs/bundle/release/*.aab
