name: BikeFit Simple V2 Fix

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🧹 V1 잔재 강제 삭제 및 설정
        run: |
          flutter clean
          # 1. 구식 경로 파일 삭제 (에러 방지)
          rm -rf android/app/src/main/kotlin/com/kimro79
          
          # 2. MainActivity.kt 생성
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

          # 3. key.properties 생성
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          
          if [ -f "release.jks" ]; then
            cp release.jks android/app/release.jks
          fi

          flutter pub get

      - name: 🚀 AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: 📦 파일 다운로드
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Fixed_AAB
          path: build/app/outputs/bundle/release/*.aab
