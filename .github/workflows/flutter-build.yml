name: BikeFit Absolute Fixed Build
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 [키 고정] android/app 내의 파일 직접 사용
        run: |
          # 1. 기존 설정 찌꺼기만 삭제 (프로젝트 코드는 보존)
          rm -f android/key.properties

          # 2. key.properties 생성
          # 사용자님이 직접 넣으신 android/app/release.jks를 그대로 바라봅니다.
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 3. 빌드 설정에서 릴리스 서명 강제 적용
          # 디버그 키가 끼어들지 못하게 빌드 스크립트만 살짝 조정합니다.
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 결과물 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_FIXED_AAB
          path: build/app/outputs/bundle/release/app-release.aab
