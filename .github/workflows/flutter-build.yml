name: BikeFit FINAL SUCCESS SDK35

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ☕ Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔥 [완성] 안드로이드 폴더 재생성 및 SDK 35 설정
        run: |
          # 1. 초기화 및 재생성
          flutter clean
          rm -rf android
          flutter create . --platforms android --org com.overthebike
          
          # 2. 경로 정보 주입 (flutterRoot 에러 방지)
          echo "sdk.dir=$ANDROID_SDK_ROOT" > android/local.properties
          echo "flutter.sdk=$FLUTTER_ROOT" >> android/local.properties

          # 3. 최신 V2 build.gradle 주입 (compileSdk 35 적용)
          cat <<EOF > android/app/build.gradle
          def localProperties = new Properties()
          def localPropertiesFile = rootProject.file('local.properties')
          if (localPropertiesFile.exists()) {
              localProperties.load(new FileInputStream(localPropertiesFile))
          }

          def flutterRoot = localProperties.getProperty('flutter.sdk')

          apply plugin: 'com.android.application'
          apply plugin: 'kotlin-android'
          apply from: "\$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"

          def keystoreProperties = new Properties()
          def keystorePropertiesFile = rootProject.file('key.properties')
          if (keystorePropertiesFile.exists()) {
              keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
          }

          android {
              namespace "com.overthebike.over_the_bike_fit"
              // 최신 플러그인 요구사항에 맞춰 35로 상향
              compileSdkVersion 35
              
              defaultConfig {
                  applicationId "com.overthebike.over_the_bike_fit"
                  minSdkVersion 21
                  targetSdkVersion 35
                  versionCode 4
                  versionName "1.0.3"
              }

              // Java 17 호환성 강제 지정
              compileOptions {
                  sourceCompatibility JavaVersion.VERSION_17
                  targetCompatibility JavaVersion.VERSION_17
              }

              kotlinOptions {
                  jvmTarget = '17'
              }

              signingConfigs {
                  release {
                      keyAlias keystoreProperties['keyAlias']
                      keyPassword keystoreProperties['keyPassword']
                      storeFile file(keystoreProperties['storeFile'])
                      storePassword keystoreProperties['storePassword']
                  }
              }

              buildTypes {
                  release {
                      signingConfig signingConfigs.release
                      minifyEnabled true
                      shrinkResources true
                      proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
                  }
              }
          }
          flutter { source '../..' }
          EOF

          # 4. 키스토어 설정 (Sodomaki80)
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          
          if [ -f "release.jks" ]; then
            cp release.jks android/app/release.jks
          fi

          flutter pub get

      - name: 🚀 AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: 📦 파일 다운로드
        uses: actions/upload-artifact@v4
        with:
          name: OverTheBike_SDK35_AAB
          path: build/app/outputs/bundle/release/*.aab
