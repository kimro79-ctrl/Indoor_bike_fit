name: BikeFit - V1 EMBEDDING KILLER SUCCESS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 17 및 Flutter 설정 (3.27.1)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Flutter 설정
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🏗️ [구조 개조] V1 임베딩 흔적 완전 소거
        run: |
          flutter clean
          
          # 1. 모든 Java 폴더와 Generated 파일을 물리적으로 삭제
          rm -rf android/app/src/main/java
          find . -name "*GeneratedPluginRegistrant*" -exec rm -rf {} + || true
          
          # 2. 최신 Kotlin 경로 및 MainActivity 강제 고정
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

          # 💡 [핵심] 프로젝트 설정에서 V1을 쓰지 않겠다고 강제 선언 (Gradle 주입)
          # 이 설정이 들어가야 엔진이 Java 파일을 다시 안 만듭니다.
          echo "flutter.minSdkVersion=21" >> android/local.properties
          echo "flutter.targetSdkVersion=34" >> android/local.properties

      - name: 🔑 [성공] 키스토어 및 서명 설정 (루트 JKS 활용)
        run: |
          # 사용자님이 메인으로 옮기신 JKS 활용
          cp release.jks android/app/release.jks
          
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

      - name: 🚀 [필살기] AAB 빌드 (V1 체크 강제 통과)
        run: |
          # pub get 단계에서 구식 플러그인이 Java를 만드는 걸 방지
          flutter pub get
          
          # 빌드 직전, 만약 Java 폴더가 다시 생겼다면 '폴더'로 막아버림 (알박기)
          rm -rf android/app/src/main/java
          mkdir -p android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java

          # --no-pub 옵션으로 빌드 도중 간섭을 완전히 차단
          flutter build appbundle --release --no-tree-shake-icons --no-pub

      - name: 📦 [완료] AAB 결과물 추출
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_AAB
          path: |
            build/app/outputs/bundle/release/*.aab
            build/**/*.aab
          if-no-files-found: error
