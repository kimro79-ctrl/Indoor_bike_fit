name: BikeFit Final Emergency Fix

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🛠️ 구식 설정 강제 삭제 및 최신 규격(V2) 주입
        run: |
          flutter clean
          
          # 1. 패키지명 고정 (over_the_bike_fit)
          PACKAGE_NAME="com.overthebike.over_the_bike_fit"
          PACKAGE_PATH="android/app/src/main/kotlin/com/overthebike/over_the_bike_fit"
          
          # 2. 에러 유발 파일들 삭제
          find android/app/src/main -name "MainActivity.*" -delete
          rm -f android/app/src/main/AndroidManifest.xml

          # 3. MainActivity.kt 새로 생성
          mkdir -p $PACKAGE_PATH
          cat <<EOF > $PACKAGE_PATH/MainActivity.kt
          package $PACKAGE_NAME
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

          # 4. AndroidManifest.xml 새로 생성 (V2 임베딩 강제 명시)
          cat <<EOF > android/app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android"
              package="$PACKAGE_NAME">
              <application
                  android:label="over_the_bike_fit"
                  android:name="\${applicationName}"
                  android:icon="@mipmap/ic_launcher">
                  <activity
                      android:name=".MainActivity"
                      android:exported="true"
                      android:launchMode="singleTop"
                      android:theme="@style/LaunchTheme"
                      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|screenLayout|density|fontScale"
                      android:hardwareAccelerated="true"
                      android:windowSoftInputMode="adjustResize">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <meta-data
                      android:name="flutterEmbedding"
                      android:value="2" />
              </application>
          </manifest>
          EOF

          # 5. 기존 release.jks 연결 및 key.properties 생성
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 6. build.gradle 설정 강제 교체 (V1 에러의 핵심 원인 제거)
          GRADLE_FILE="android/app/build.gradle"
          if [ -f "$GRADLE_FILE" ]; then
            # 패키지명 일치 및 서명 설정 적용
            sed -i "s/applicationId \".*\"/applicationId \"$PACKAGE_NAME\"/g" $GRADLE_FILE
            sed -i "s/namespace \".*\"/namespace \"$PACKAGE_NAME\"/g" $GRADLE_FILE
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' $GRADLE_FILE
          fi

          flutter pub get

      - name: AAB 빌드 실행 (V2 규격 적용)
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: 📦 ★최종 AAB 파일 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_FINAL_AAB
          path: build/app/outputs/bundle/release/*.aab
          if-no-files-found: error
