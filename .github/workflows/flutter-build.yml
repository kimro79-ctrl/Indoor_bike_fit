name: BikeFit Final Success Build
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 [v1 오류 해결 및 키 복원]
        run: |
          # 1. 먼저 메인에 있는 키를 안전한 임시 장소(/tmp)에 복사해둡니다.
          cp android/app/release.jks /tmp/release.jks || cp release.jks /tmp/release.jks || echo "No jks found"

          # 2. 최신 구조로 갱신 (v1 embedding 해결)
          # 사용자님의 패키지명 com.overthebike를 유지합니다.
          flutter create . --platforms android --org com.overthebike --overwrite

          # 3. 덮어쓰기로 인해 지워진 release.jks를 다시 android/app/으로 복원
          mkdir -p android/app
          cp /tmp/release.jks android/app/release.jks

          # 4. key.properties 설정 재주입
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 5. debug 대신 release 서명 강제 적용
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 결과물 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_V1_FIXED_AAB
          path: build/app/outputs/bundle/release/app-release.aab
