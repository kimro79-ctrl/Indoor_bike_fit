name: BikeFit Google Console Final Success
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 임시 키 생성 원천 차단 및 서명 고정
        run: |
          # 1. 기존 빌드 찌꺼기 및 캐시 완전 삭제
          flutter clean
          rm -rf android/.gradle
          rm -rf android/app/build.gradle.bak

          # 2. 안드로이드 설정 최신화 (V1 에러 방지)
          flutter create . --platforms android --overwrite

          # 3. 사용자 JKS 파일 복사
          mkdir -p android/app
          cp release.jks android/app/release.jks || (echo "❌ JKS 파일이 루트에 없습니다!" && exit 1)

          # 4. key.properties 생성 (fresh-upload-key 고정)
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 5. [핵심] build.gradle 수정: 디버그 서명을 아예 삭제하고 릴리스 서명만 강제
          # 이 작업이 없으면 키를 못 찾을 때 빌드 서버가 임시 키를 멋대로 찍습니다.
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행 (임시 키 사용 시 실패하도록 설정)
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 AAB 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_SUCCESS_AAB
          path: build/app/outputs/bundle/release/app-release.aab
