name: BikeFit FINAL V2 Auto-Migration Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout source
        uses: actions/checkout@v4

      - name: ☕ Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Setup Flutter (안정판)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'

      - name: 🧹 Clean environment & auto-migrate Android V2
        run: |
          echo "🧼 Cleaning old cache and build files..."
          flutter clean
          rm -rf ~/.pub-cache
          flutter pub cache repair || true

          echo "📦 Fetching dependencies..."
          flutter pub get

          echo "🧩 Regenerating Android project with V2 embedding..."
          rm -rf android
          flutter create .

          echo "✅ Android project regenerated with V2 embedding."
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity() {}
          EOF

          echo "✅ MainActivity.kt created successfully."

      - name: 🔐 Configure release keystore
        run: |
          echo "🔑 Setting up keystore..."
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          
          if [ ! -f "android/app/release.jks" ]; then
            cp $GITHUB_WORKSPACE/release.jks android/app/release.jks
          fi

      - name: 🔍 Display SHA1 fingerprint
        run: |
          echo "🔍 Extracting SHA1 fingerprint..."
          keytool -list -v \
            -keystore android/app/release.jks \
            -alias fresh-upload-key \
            -storepass Sodomaki80 \
            -keypass Sodomaki80 | grep "SHA1"

      - name: 🚀 Build Android App Bundle (AAB)
        run: |
          echo "🚀 Building release AAB..."
          flutter build appbundle --release --no-tree-shake-icons

      - name: 📦 Upload final AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_FINAL_V2_AAB
          path: build/app/outputs/bundle/release/*.aab
