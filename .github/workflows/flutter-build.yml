name: BikeFit - Restore Success & Get AAB

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 17 및 Flutter 설정
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Flutter 설정 (안정 버전)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🧹 빌드 환경 초기화 및 V1 좀비 제거
        run: |
          flutter clean
          # 빌드 에러를 유발하는 자동 생성 폴더 삭제
          rm -rf android/app/src/main/java
          
          # MainActivity.kt 위치 강제 고정
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

      - name: 🔑 [중요] 키스토어 파일 배치
        run: |
          # jks 파일이 프로젝트 루트에 있다면 android/app 폴더로 복사합니다.
          cp release.jks android/app/release.jks || echo "JKS already in place"
          
          # 서명 정보 작성
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

      - name: 📦 의존성 해결 및 빌드 실행
        run: |
          flutter pub get
          # 빌드 직전 다시 한번 V1 잔재 삭제
          rm -rf android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          # --no-tree-shake-icons 옵션으로 안전하게 빌드
          flutter build appbundle --release --no-tree-shake-icons

      - name: 🚀 [성공 결과물] AAB 파일 다운로드
        uses: actions/upload-artifact@v4
        with:
          name: OverTheBike_Final_AAB
          # 빌드된 모든 위치에서 AAB를 찾음
          path: build/app/outputs/bundle/release/*.aab
          if-no-files-found: error
