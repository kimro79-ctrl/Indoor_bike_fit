name: BikeFit - ABSOLUTE FINAL SUCCESS (V1 BYPASS)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 17 및 Flutter 설정
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Flutter 설정 (3.27.1)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🏗️ [구조 정상화] Java/Kotlin 경로 최적화
        run: |
          flutter clean
          # 1. 기존의 잘못된 'java 파일'이 있다면 확실히 제거
          rm -rf android/app/src/main/java
          
          # 2. 정석적인 Java 패키지 경로 생성
          mkdir -p android/app/src/main/java/io/flutter/plugins
          
          # 💡 [핵심 전략] 에러 파일이 생성될 자리에 미리 '폴더'를 만들어 생성을 방해함
          # 파일이 생성되려 할 때 "이미 같은 이름의 디렉토리가 있음" 에러를 내며 생성이 무산됩니다.
          mkdir -p android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java

      - name: 🔑 [성공] 키스토어 및 서명 설정
        run: |
          # 루트에 있는 release.jks를 활용
          cp release.jks android/app/release.jks
          
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

      - name: 🚀 AAB 빌드 실행 (V1 우회 필살기)
        run: |
          flutter pub get
          
          # 빌드 직전, 다시 한 번 해당 경로가 디렉토리인지 확인 (강력 보호)
          # 만약 파일로 생겨났다면 삭제하고 다시 디렉토리로 만듭니다.
          rm -f android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          mkdir -p android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          
          # --no-pub으로 빌드 중 엔진의 파일 간섭 차단
          flutter build appbundle --release --no-tree-shake-icons --no-pub

      - name: 📦 [완료] AAB 결과물 추출
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_AAB
          path: |
            build/app/outputs/bundle/release/*.aab
            build/**/*.aab
          if-no-files-found: error
