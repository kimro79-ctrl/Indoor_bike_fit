name: BikeFit - ABSOLUTE FINAL SUCCESS (V1 BYPASS)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 17 설정
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Flutter 설정 (3.27.1)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔥 [결정적 조치] V1 좀비 경로 물리적 폐쇄
        run: |
          flutter clean
          
          # 1. 구식 Java 폴더 전체 삭제
          rm -rf android/app/src/main/java
          
          # 2. 최신 Kotlin 경로 생성 및 MainActivity 배치
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

          # 💡 [핵심] java라는 이름의 '파일'을 만들어 폴더 생성을 막음
          # 플러터가 java 폴더를 만들려 할 때 이미 같은 이름의 파일이 있어 실패하게 됩니다.
          touch android/app/src/main/java 

      - name: 🛠️ 빌드 환경 및 키스토어 설정
        run: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > android/local.properties
          echo "flutter.sdk=$FLUTTER_ROOT" >> android/local.properties

          # 서명 정보 주입
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          
          # JKS 파일 위치 고정
          cp release.jks android/app/release.jks || echo "JKS check"

      - name: 🚀 AAB 빌드 실행 (V1 체크 강제 우회)
        run: |
          # pub get을 먼저 수행
          flutter pub get
          
          # 빌드 직전, 다시 한번 java 경로가 파일인지 확인 (강력 보호)
          rm -rf android/app/src/main/java
          touch android/app/src/main/java
          
          # --no-pub 옵션으로 빌드 중 간섭 차단
          flutter build appbundle --release --no-tree-shake-icons --no-pub

      - name: 📦 [성공] AAB 결과물 추출
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_AAB
          path: |
            build/app/outputs/bundle/release/*.aab
            build/**/*.aab
          if-no-files-found: error
