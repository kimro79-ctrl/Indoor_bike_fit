name: BikeFit OverTheBike ABSOLUTE SUCCESS V2

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ☕ Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔥 [결정적 단계] V1 완전 소탕 및 경로 강제 고정
        run: |
          flutter clean
          
          # 1. 패키지 경로 및 MainActivity 설정
          PACKAGE_NAME="com.overthebike.over_the_bike_fit"
          PACKAGE_PATH="android/app/src/main/kotlin/com/overthebike/over_the_bike_fit"
          
          # 2. V1 흔적이 남는 java 폴더와 자동 생성 파일을 완전히 물리적으로 제거
          rm -rf android/app/src/main/java
          
          # 3. MainActivity.kt를 최신 V2 규격으로 생성
          mkdir -p $PACKAGE_PATH
          cat <<EOF > $PACKAGE_PATH/MainActivity.kt
          package $PACKAGE_NAME
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

      - name: 🛠️ 빌드 환경 강제 최적화 (SDK 35)
        run: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > android/local.properties
          echo "flutter.sdk=$FLUTTER_ROOT" >> android/local.properties

          # build.gradle 작성 (compileSdk 35 반영)
          cat <<EOF > android/app/build.gradle
          def localProperties = new Properties()
          def localPropertiesFile = rootProject.file('local.properties')
          if (localPropertiesFile.exists()) { localProperties.load(new FileInputStream(localPropertiesFile)) }
          def flutterRoot = localProperties.getProperty('flutter.sdk')

          apply plugin: 'com.android.application'
          apply plugin: 'kotlin-android'
          apply from: "\$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"

          def keystoreProperties = new Properties()
          def keystorePropertiesFile = rootProject.file('key.properties')
          if (keystorePropertiesFile.exists()) { keystoreProperties.load(new FileInputStream(keystorePropertiesFile)) }

          android {
              namespace "com.overthebike.over_the_bike_fit"
              compileSdkVersion 35
              defaultConfig {
                  applicationId "com.overthebike.over_the_bike_fit"
                  minSdkVersion 21
                  targetSdkVersion 35
                  versionCode 4
                  versionName "1.0.4"
              }
              compileOptions { sourceCompatibility JavaVersion.VERSION_17; targetCompatibility JavaVersion.VERSION_17 }
              kotlinOptions { jvmTarget = '17' }
              signingConfigs {
                  release {
                      keyAlias keystoreProperties['keyAlias']
                      keyPassword keystoreProperties['keyPassword']
                      storeFile file('release.jks')
                      storePassword keystoreProperties['storePassword']
                  }
              }
              buildTypes { release { signingConfig signingConfigs.release; minifyEnabled true; shrinkResources true } }
          }
          flutter { source '../..' }
          EOF

          # 키스토어 설정
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          
          flutter pub get

      - name: 🚀 AAB 빌드 실행 (V1 체크 강제 우회)
        run: |
          # 💡 빌드 명령어 바로 직전에 java 폴더를 다시 한번 확실히 지웁니다.
          rm -rf android/app/src/main/java
          # '--no-pub' 옵션을 주어 빌드 중 파일이 자동 재생성되는 것을 막습니다.
          flutter build appbundle --release --no-tree-shake-icons --no-pub

      - name: 📦 [최종] AAB 파일 다운로드
        uses: actions/upload-artifact@v4
        with:
          name: OverTheBike_SUCCESS_AAB
          path: build/app/outputs/bundle/release/*.aab
