name: BikeFit Final Success Build
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 기존 JKS 키 연결 및 서명 설정
        run: |
          echo "🔑 release.jks 복사 중..."
          mkdir -p android/app
          # 루트에 있는 release.jks를 복사
          cp release.jks android/app/release.jks || echo "⚠️ JKS 파일 복사 확인 필요"

          echo "🧩 key.properties 생성"
          # storeFile 경로를 app/release.jks로 적어주는 것이 가장 안전합니다.
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=app/release.jks
          EOF

          echo "🛠 build.gradle 서명 설정 수정"
          if [ -f "android/app/build.gradle" ]; then
            # debug 서명을 release 서명으로 강제 교체
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 AAB 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_SUCCESS_AAB
          path: build/app/outputs/bundle/release/app-release.aab
