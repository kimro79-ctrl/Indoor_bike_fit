name: BikeFit Final Signing Fix
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'

      - name: Hard Reset & Force Signing
        run: |
          # 1. 이전 찌꺼기 제거
          flutter clean
          rm -f android/key.properties
          
          # 2. 루트에 있는 release.jks를 안드로이드 앱 폴더로 강제 복사
          mkdir -p android/app
          cp release.jks android/app/release.jks || exit 1

          # 3. key.properties 설정 강제 생성
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

      - name: Build App Bundle
        run: |
          flutter pub get
          flutter build appbundle --release --verbose

      - name: 🚨 지문 검증 (이걸 확인하세요)
        run: |
          echo "현재 빌드된 파일의 SHA1 지문입니다:"
          keytool -printcert -jarfile build/app/outputs/bundle/release/app-release.aab | grep SHA1
          echo "위 값이 구글 콘솔의 75:CD...와 똑같아야 합니다."

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_AAB
          path: build/app/outputs/bundle/release/app-release.aab
