name: BikeFit - FINAL DEPLOY SUCCESS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 17 및 Flutter 설정
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Flutter 설정 (3.27.1)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🏗️ [구조 정상화] Java 폴더 정석 생성
        run: |
          flutter clean
          # 1. 혹시 남아있을지 모를 'java 파일'을 제거 (에러 방지)
          rm -f android/app/src/main/java
          
          # 2. 정석적인 Java 패키지 경로 생성 (엔진 요구사항 충족)
          mkdir -p android/app/src/main/java/io/flutter/plugins
          
          # 3. Kotlin 소스 위치 고정
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit

      - name: 🔑 [성공] 키스토어 및 서명 설정
        run: |
          # 💡 루트에 있는 release.jks를 빌드 폴더로 복사
          cp release.jks android/app/release.jks
          
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

      - name: 🚀 AAB 빌드 실행 (V1 완벽 차단)
        run: |
          flutter pub get
          # 💡 pub get 직후, 자동으로 생성된 구식 파일을 삭제하여 V1 에러를 방어합니다.
          rm -f android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          
          # --no-pub 옵션으로 빌드 중 엔진의 파일 생성을 원천 차단
          flutter build appbundle --release --no-tree-shake-icons --no-pub

      - name: 📦 [성공] AAB 결과물 추출
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_AAB
          path: |
            build/app/outputs/bundle/release/*.aab
            build/**/*.aab
          if-no-files-found: error
