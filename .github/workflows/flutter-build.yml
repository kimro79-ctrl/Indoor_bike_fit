name: BikeFit Final Ultra Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: 🐦 Flutter 설정 (3.27.1)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🏗️ 환경 재생성 (성공 로직)
        run: |
          # 1. 기존 android 폴더 제거 후 최신 구조(V2)로 재건
          rm -rf android
          flutter create . --platforms android --org com.overthebike --project-name over_the_bike_fit
          flutter pub get
          
          # 2. V2 선언이 포함된 AndroidManifest 설정
          cat <<EOF > android/app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.overthebike.over_the_bike_fit">
              <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
              <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
              <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
              <application android:label="Indoor_fit_app" android:icon="@mipmap/ic_launcher">
                  <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop" android:theme="@style/LaunchTheme">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <meta-data android:name="flutterEmbedding" android:value="2" />
              </application>
          </manifest>
          EOF
          
          # 3. MultiDex 활성화
          sed -i '/defaultConfig {/a \        multiDexEnabled true' android/app/build.gradle

      - name: 🚀 Build Unsigned AAB
        # 중복 서명 방지를 위해 순수 빌드 진행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: 🖋️ Sign AAB with New Upload Key
        run: |
          # 💡 [핵심] 사용자님이 메인에 올려두신 '새 업로드 키'를 바로 사용합니다.
          # 파일: release.jks / 비밀번호: Sodomaki80 / 별칭: fresh-upload-key
          jarsigner -keystore release.jks \
          -storepass Sodomaki80 -keypass Sodomaki80 \
          build/app/outputs/bundle/release/app-release.aab fresh-upload-key

      - name: 📦 Upload Final AAB
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Update_AAB_Success
          path: build/app/outputs/bundle/release/app-release.aab
