name: BikeFit - AAB GENERATION (V1 BYPASS)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 17 설정
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Flutter 설정 (3.27.1)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔥 [1단계] V1 구조 파괴 및 최신 구조 재편
        run: |
          flutter clean
          # 구식 Java 폴더 전체 삭제
          rm -rf android/app/src/main/java
          
          # 최신 Kotlin 경로 생성 및 MainActivity 배치
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

      - name: 🛠️ [2단계] 빌드 환경 및 키스토어 설정
        run: |
          # SDK 경로 설정
          echo "sdk.dir=$ANDROID_SDK_ROOT" > android/local.properties
          echo "flutter.sdk=$FLUTTER_ROOT" >> android/local.properties

          # 서명 정보 주입 (Sodomaki80)
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          
          # JKS 파일 위치 고정
          cp release.jks android/app/release.jks || echo "JKS already exists"

      - name: 🚀 [3단계] 필살기 - AAB 빌드 (V1 체크 우회)
        run: |
          flutter pub get
          # pub get 이후에 다시 생성되었을지 모를 V1 파일을 마지막으로 제거
          rm -rf android/app/src/main/java
          
          # --no-pub 옵션으로 빌드 중 파일 재생성 원천 봉쇄
          flutter build appbundle --release --no-tree-shake-icons --no-pub

      - name: 📦 [완료] AAB 파일 업로드
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_AAB
          # 빌드된 파일이 있는 모든 경로를 탐색하여 가져옵니다.
          path: |
            build/app/outputs/bundle/release/*.aab
            build/**/*.aab
          if-no-files-found: error
