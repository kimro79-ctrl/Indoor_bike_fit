      - name: Generate PEM for Google
        run: |
          keytool -export -rfc -keystore my-release-key.jks -alias my-key-alias -file upload_certificate.pem -storepass Sodomaki79
      - name: Upload PEM Artifact
        uses: actions/upload-artifact@v4
        with:
          name: google-reset-file
          path: upload_certificate.pem
