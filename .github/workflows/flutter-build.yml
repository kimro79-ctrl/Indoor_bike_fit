name: BikeFit Key-Safe Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 V1 에러 방지 및 릴리스 서명 설정
        run: |
          # 1. 빌드 캐시 청소
          flutter clean

          # 2. 패키지 경로 및 MainActivity.kt 재생성 (Indoor_bike_fit 프로젝트 기준)
          find android/app/src/main -name "MainActivity.*" -delete
          mkdir -p android/app/src/main/kotlin/com/kimro79/indoor_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/kimro79/indoor_bike_fit/MainActivity.kt
          package com.kimro79.indoor_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

          # 3. key.properties 생성 (android/app/ 경로의 jks 참조)
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 4. build.gradle 설정 강제 수정 (V1 에러 방지를 위해 release 서명 강제 적용)
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행 (V1/V2 서명 문제를 해결하는 AAB 방식)
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 AAB 파일 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_FINAL_AAB
          path: build/app/outputs/bundle/release/app-release.aab
